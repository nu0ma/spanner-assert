/**
 * Default port for Cloud Spanner emulator gRPC endpoint.
 * @see https://cloud.google.com/spanner/docs/emulator
 */
export const DEFAULT_EMULATOR_PORT = 9010;

/**
 * Number of context lines to show in diff output when comparing expected vs actual values.
 */
export const DIFF_CONTEXT_LINES = 3;

/**
 * JSON indentation spaces for formatting error messages.
 */
export const JSON_INDENT_SPACES = 2;

